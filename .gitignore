bin/
*.test
*.out
coverage.txt
.idea/
.vscode/
.DS_Store

# Local-dev secrets (template lives at deploy/local/.env.example).
deploy/local/.env

# air autoreload working dir (lives at the repo root, populated inside
# the dev container too).
.air-tmp/
